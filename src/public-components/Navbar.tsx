import { Flex } from '@chakra-ui/react';

function Navbar() {
  return (
    <>
      <Flex
        as="header"
        align="center"
        justify="space-between"
        padding="1rem"
        bg="#ffffff"
        position="sticky"
        backgroundColor={'teal'}
        top={0}
      >
        
      </Flex>
    </>
  );
}

export default Navbar;
